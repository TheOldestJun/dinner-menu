/* import Dexie from 'dexie';

export const db = new Dexie('dinnerDB');
db.version(1).stores({
  meals_soup: 'id, &title',
  meals_garnish: 'id, &title',
  meals_meat: 'id, &title',
  meals_salad: 'id, &title',
  meals_bakery: 'id, &title',
  meals_drink: 'id, &title',
  menu: 'id, monday, tuesday, wednesday, thursday, friday, dateFirst, dateLast',
});

export default db; */